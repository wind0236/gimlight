module UI.Draw.Config
    ( tileWidth
    , tileHeight
    , tileColumns
    , tileRows
    , logRows
    , windowWidth
    , windowHeight
    ) where

tileWidth, tileHeight :: Int
tileWidth = 48
tileHeight = 48

tileColumns, tileRows :: Int
tileColumns = 23
tileRows = 13

logRows :: Int
logRows = 5

windowWidth, windowHeight :: Int
windowWidth = 1280
windowHeight = 720
